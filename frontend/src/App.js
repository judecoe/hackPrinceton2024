import React from 'react';
import Mask from './Mask';

const App = () => {
    return (
        <div className="App">
            {/* Mask Component with Mapbox Map */}
            <Mask />
        </div>
    );
};

export default App;
